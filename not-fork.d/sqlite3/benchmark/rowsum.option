# "rowsum" build option

# this option controls whether the build process includes rowsum code:
# the default rowsum algorithm is selected by build.mk but can be
# changed with PRAGMAs

# this option affects the build process
build = yes

# a Boolean: either we include the code, or we don't
boolean

# default value if not specified
default = off

