# "datasize" benchmark option

# when running tests, some of the data is specified as a base number
# multiplied by the value of this options (default 1)

# see tool/build.tcl for how this information is used

# this option does not affect the build process - runtime only
build = no

# takes 1 or 2 numeric values
syntax = [1-9]\d*(?:,[1-9]\d*)?

# default value if not specified
default = 1

# if two values are equal, we just want the one
requiv = (\d+),\1 \1

