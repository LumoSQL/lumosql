set d26000 [expr $options(DATASIZE) * 26000]
set d51000 [expr $options(DATASIZE) * 51000]

set name "Row count with an index"

# repeat this a few time otherwise it's too fast to measure properly
for {set i 0} {$i < 1000} {incr i} {
  append sql "SELECT COUNT(*) FROM t1;\n"
  append sql "SELECT COUNT(*) FROM t2;\n"
}

# but only need to check first result
lappend results "^$d26000\$" "^$d51000\$"

